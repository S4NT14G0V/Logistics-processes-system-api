type InventorySummaryResponse {
    region: String!
    inTransit: Int!
    delivered: Int!
    pending: Int!
}

extend type Query {
    inventorySummary(periodStart: String, periodEnd: String, region: String): [InventorySummaryResponse!]!
}
